# 风格指纹

> 本文件由 skill 自动生成，**请勿手动填写**。
>
> 首次指定范例论文时（对话里说「参考论文是 /path/to/thesis.pdf」），skill 会
> Read 一次原文，抽出 5–10 KB 的风格指纹写进本文件。此后所有对话默认只加载
> 本文件，不再碰范例原文，token 成本大幅下降。
>
> 当前状态：**尚未配置范例论文**，下面是待填充的骨架。

---

## 元信息

- 路径：`<范例论文 PDF 路径>`
- 作者：`<姓名（单位，导师）>`
- 方向/学科：`<学科 / 研究方向>`
- 页数：`<N>`，`<年月>`
- 指纹生成日期：`<YYYY-MM-DD>`

---

## 章引言样例（`X.1 引言`）

**范例 `<章号.节号>` 引言**（`<类型>`，约 `<N>` 字）

> `<逐字摘录>`

**套路提炼**

- `<单段还是分点，多少字>`
- `<结构：如何起、如何转折、如何收>`
- `<结尾是否有「本章将…」>`

---

## 方法章叙述样例

**范例 `<章.节>`**（`<说明这是什么样例>`）

> `<逐字摘录>`

**套路提炼**

- `<公式如何引入：先说假设、再给式，还是先给式再解释>`
- `<公式前后的文字长度>`

---

## 公式叙述风格

- `<公式编号方式>`、`<引用方式：式(3.1) 还是 式 3.1>`
- `<符号说明放在式前还是式后>`

---

## 本章小结样例

> `<逐字摘录>`

**套路提炼**

- `<一段话写完，还是分条；多少字>`
- `<是否重复本章内容，还是给结论>`

---

## 图题样例

- `<格式：图 3-1 还是 图 3.1；有无句号>`
- `<子图 (a)(b)(c) 说明放图题还是正文>`
- `<图题长度>`

---

## 表题样例

- `<格式与位置：表上方还是下方>`
- `<三线表还是全线表>`
- `<表内文字的号数>`

---

## 高频术语与句式

| 场景 | 范例用法 |
|---|---|
| `<场景>` | `<句式>` |

**注意**：范例若使用了本论文的禁用词（见 `local-constraints.md` 第二节），
**以约束层为准，不跟着范例走**。

---

## 小节命名风格

- `<动宾结构还是名词短语>`
- `<长度>`
- `<是否允许自造复合术语>`

---

## 实验章写法

- `<实验设置 → 性能评估 的结构>`
- `<评估如何分小节>`
- `<表格与正文的分工>`

---

## 指纹生成者备注

- `<抽取时用到的命令，如 pdftotext -layout>`
- `<文本质量问题，如公式乱码、表格错位>`
- `<术语冲突提醒>`
